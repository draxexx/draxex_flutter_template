import 'package:flutter/material.dart';

class DraxexDivider extends StatelessWidget {
  const DraxexDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider();
  }
}
